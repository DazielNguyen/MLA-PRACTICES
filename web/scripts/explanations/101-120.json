{
"101":["Checkpointable queued GPU jobs with flexible completion windows can use managed batch scheduling on interruptible capacity.","Batch manages queues and compute provisioning, while GPU-capable Spot instances reduce cost. Saved checkpoints let interrupted jobs resume within the weekly window.","Scheduling a single EC2 instance's uptime does not provide the same managed job queue, retries, or elastic GPU capacity.","Fargate does not provide the GPU resource required by this TensorFlow workload.","An ECS service scheduler maintains service tasks; it is less direct than Batch job scheduling for queued, finite, restartable GPU work."],
"102":["The elbow method chooses a cluster count where further clusters yield diminishing reductions in within-cluster error.","One cluster gives a high error and ignores the substantial improvements visible before the elbow.","The curve flattens around k = 4, balancing the large initial error reduction against additional cluster complexity.","Seven clusters lie beyond the main elbow, where extra clusters provide relatively small improvement.","Ten minimizes error among the tested counts but adds clusters after diminishing returns; minimum error alone is not the elbow criterion."],
"103":["Pretrained managed AI services can annotate multiple media types without first building custom models.","Rekognition analyzes visual media, Transcribe turns speech into searchable text, and Comprehend extracts text information, enabling rapid metadata generation.","Manual labeling of the entire archive is labor-intensive and does not exploit available pretrained models for the fastest initial indexing.","Training and operating topic and object-detection models adds data preparation and ML expertise requirements.","Custom transcription and vision models require substantial development, training data, and GPU operations compared with managed pretrained APIs."],
"104":["Provisioned stream capacity must satisfy both records-per-second and bytes-per-second limits, with appropriate partition-key distribution.","One standard shard's 1 MB/s write capacity is insufficient for roughly 10 MB/s of incoming payloads.","100 records/s × 100 KB is about 10 MB/s; ten standard 1 MB/s shards meet the stated minimum byte throughput, assuming evenly distributed writes.","The record count does not require one shard per record; this greatly exceeds the capacity implied by the stated load.","A thousand shards are unnecessary for the requested throughput and would not represent the minimum."],
"105":["Naive Bayes assumes feature independence conditional on the class, which differs from marginal Pearson correlation.","The supplied correlations do not establish conditional independence; strong marginal correlation can still arise through a shared class label.","A full Bayesian network can represent dependencies, but this explanation incorrectly asserts that all features are conditionally independent.","Ordinary Naive Bayes does not explicitly model feature-to-feature dependence conditional on the class, so this is not a valid justification for it.","This is the intended answer if relevant conditional dependencies exist. However, marginal correlation alone does not prove that condition, so the key remains qualified."],
"106":["A log transform can reduce right skew, but linear regression does not require normally distributed input features.","Exponentiation generally magnifies a positive right tail, and does not address the question's mistaken predictor-normality premise.","Logarithms can compress a positive right-skewed feature. That may help modeling, but it does not by itself establish the residual assumptions needed for coefficient inference.","Polynomial features model nonlinear relationships and may magnify skew; they do not generally make a predictor normal.","A sinusoidal transform is appropriate for certain cyclic structures, not generic right-skew correction or validation of regression assumptions."],
"107":["Limiting tree depth reduces the complexity of a boosted-tree model.","Deeper trees can fit finer training-specific patterns and worsen overfitting.","Lowering max_depth limits tree complexity and is the direct regularization adjustment among the choices.","Changing the objective defines the prediction task; it is not the stated remedy for an already functioning binary fraud classifier's overfitting.","Lower min_child_weight permits smaller, more specific leaves and can make the model more prone to overfitting."],
"108":["A streaming anomaly detector must adapt to recent observations rather than remain fixed on historical training data.","An RCF model trained once on history can score records, but the option omits retraining or another mechanism to adapt to changing patterns.","The proposed XGBoost workflow lacks labeled anomaly targets and does not define an adaptive unsupervised detector.","The historical SQL anomaly function was RCF, not the described kNN extension; tumbling windows also reset rather than maintain the proposed sliding context.","Historically, streaming RCF used recent observations to update anomaly scoring while processing incoming events. This answer applies to the retired Kinesis SQL service."],
"109":["Aggregate outcomes into monthly counts per category, then forecast each category's future count series.","Classifying individual claims is a different target from predicting the number of claims that will occur next month.","There is no sequential action/reward environment requiring reinforcement learning.","Dates and category labels allow monthly count series to be built and forecast; claim IDs identify records rather than serving as meaningful numeric predictors.","Splitting categories by availability of text features changes the task unnecessarily; the required output is a count forecast for every category."],
"110":["SageMaker production variants support weighted traffic allocation among model versions behind one endpoint.","Separate endpoints work but require application-level routing and more endpoint management.","Multiple production variants share one endpoint, and variant weights control the inference split; weights/capacity can be adjusted without writing a custom router.","Neo compiles models for deployment targets; it is not the mechanism for weighted long-running model experiments.","Batch Transform runs offline inference jobs and does not route live traffic between models behind an endpoint."],
"111":["Object detection predicts both object classes and bounding boxes; image classification only labels the overall image.","An image classifier does not return the locations of individual weeds within the camera image.","The built-in SSD input contract does not accept arbitrary Parquet image records as proposed.","The built-in SSD detector supports the required class-plus-location output, with appropriately encoded image and bounding-box annotations in supported RecordIO input.","This combines the wrong task output with an unsupported proposed image input format."],
"112":["Local edge inference maintains low latency when cloud connectivity is unreliable.","A cloud endpoint depends on successful network communication for each inference, conflicting with the unreliable internet connections.","A compatible model deployed through Greengrass can process sensor data inside each factory, maintaining inference without a continuous cloud round trip.","Daily batch reports do not meet the near-real-time maintenance detection requirement.","The DynamoDB/Lambda/cloud-endpoint chain still needs internet connectivity and adds network hops rather than enabling local inference."],
"113":["SageMaker framework script mode can use existing TensorFlow data formats instead of requiring a built-in algorithm's serialization.","A local notebook path is not directly available to a remote training instance unless the data is uploaded or otherwise made accessible.","Store the TFRecords in S3 and use TensorFlow script mode, preserving the existing parsing logic; configure the script to use the provided training-channel path.","Converting an already supported TFRecord dataset adds development work and is unnecessary for TensorFlow script mode.","There is no single universal SageMaker input format requiring this ETL rewrite; the existing TensorFlow reader can consume TFRecords."],
"114":["Convolutional networks learn spatial visual features useful for recognizing objects, logos, and appearance in images.","LDA models latent topics in document-like count data rather than spatial image appearance.","RNNs primarily model sequences; they are not the most direct visual feature extractor among the options.","k-means performs unsupervised grouping and does not directly learn the supplied brand-presence labels.","A CNN can learn visual patterns associated with the retail brand from labeled images and predict whether it is present."],
"115":["Fresh interaction events allow recommendations to reflect behavior after a campaign starts.","The event tracker records new user interactions, giving Personalize information about current interests that the pre-campaign dataset lacks.","Additional static metadata does not replace missing interactions from the new campaign, and the named recipe is historical.","Replacing Personalize with a custom model adds work without directly fixing the lack of fresh training/interaction information.","Adding columns to old interaction records does not create the missing post-launch user behavior data."],
"116":["Interface endpoint security combines principal-level policy with network access controls on endpoint interfaces.","An endpoint policy can restrict which IAM principals and actions may use that endpoint, alongside normal IAM authorization.","Restricting actions in user IAM policies does not identify the permitted source instances or replace a restrictive endpoint policy.","The endpoint ENI's security group can allow connections only from designated instance security groups or network ranges.","Network ACLs apply to subnets, not directly to individual endpoint network interfaces as stated.","The Runtime endpoint serves inference calls; adding it does not secure access to the already configured SageMaker Service API endpoint."],
"117":["Data residency must be enforced before transfer, and the specified tunnel must provide IPsec.","Read only nonsensitive tables at the on-premises source over Site-to-Site VPN, which supplies IPsec, then write the allowed data to S3.","Removing sensitive fields in a cloud Glue job occurs after they have left the data center, violating the residency requirement.","SSL/TLS encrypts the database connection but is not the explicitly required IPsec tunnel.","Replicating all records to EC2 transfers sensitive data out of the data center before filtering, even if the connection is encrypted."],
"118":["Algorithm selection and hyperparameter tuning are distinct ways to improve a forecast, but this legacy API forbids enabling both modes together.","AutoML can compare supported forecasting algorithms. It is a plausible alternative experiment, but cannot be enabled together with PerformHPO in one CreatePredictor request.","Increasing the horizon to four periods changes the forecasting objective; it does not directly improve the requested next-month accuracy.","Changing to weekly frequency does not create weekly information from monthly observations and changes the task resolution.","HPO can tune a selected algorithm, but the API restriction prevents combining it with PerformAutoML in the same call as the question suggests.","Filling addresses missing values; the dataset is explicitly complete, so this does not target the described error."],
"119":["Forecast separates the target time series from static item metadata and imports supported datasets from S3.","Glue can create the required target-series CSV and static item-metadata CSV in S3 for the historical Forecast import workflow.","A related time series does not replace the required target series, and Aurora tables are not the proposed dataset import source.","Forecast imports dataset files from supported S3 locations, not directly from local machine files as stated.","RecordIO protobuf is not the Forecast CSV dataset format and does not replace its required dataset schemas."],
"120":["Inference accelerator capacity should match the model's actual demand rather than leaving a large GPU mostly idle.","Batch Transform changes real-time serving to asynchronous batch inference, failing the production application's requirement.","Historically, a CPU instance with a smaller Elastic Inference accelerator reduced unused GPU capacity. Elastic Inference is retired, so this is not a current architecture recommendation.","A larger GPU instance increases provisioned capacity despite the observed low utilization.","Moving the same P3 capacity to ECS changes the hosting platform but does not solve the unused GPU capacity." ]
}
