{
  "506": [
    "A baseline drift violation requires investigation. Replacing a baseline is justified only after confirming that the new data represents an intended, valid population change.",
    "Retraining and checking training errors do not identify whether the production data is corrupt, incorrectly transformed, or legitimately different.",
    "Keeping the old baseline is appropriate if it still defines valid data, but retraining alone will not remove an input distribution violation.",
    "This is appropriate only after validating the new distribution and the retrained model. Blindly replacing a baseline can hide a broken data pipeline, so the source answer is conditional on an investigation absent from the question.",
    "emit_metrics enables metric publication. It does not repair the data or change the constraints that produced the violations."
  ],
  "507": [
    "Recall measures the fraction of actual positive cases that are detected. Here, the positive class is fraud.",
    "F1 balances precision and recall. The stated priority is specifically to miss as few fraudulent transactions as possible.",
    "ROC AUC evaluates ranking across thresholds. It does not directly report the fraction of fraud detected at the deployed threshold.",
    "Precision measures how many flagged transactions are actually fraudulent. High precision can coexist with many missed fraud cases.",
    "Recall = true positives / (true positives + false negatives). Maximizing it directly targets catching the largest fraction of fraud, while separately monitoring the false-positive burden."
  ],
  "508": [
    "SageMaker endpoint auto scaling uses Application Auto Scaling to adjust managed serving capacity according to load or a schedule.",
    "Configure endpoint auto scaling and appropriate minimum, maximum, and target values. For a known surge, pre-scale or schedule extra capacity so initialization does not delay availability.",
    "A second endpoint does not automatically receive traffic or scale the existing application; routing and capacity management would still be missing.",
    "Neo may improve supported model execution efficiency, but compilation does not automatically add capacity when request volume increases.",
    "An EC2 Auto Scaling group is not attached directly to SageMaker-managed endpoint instances. Use SageMaker's Application Auto Scaling integration."
  ],
  "509": [
    "L1 regularization penalizes the absolute values of model coefficients and can drive some coefficients to zero, reducing reliance on unnecessary features.",
    "Additional iterations can fit training noise more closely and worsen overfitting.",
    "Add an L1 penalty to the model's training objective and retrain. The penalty applies to model parameters, rather than being a transformation of the raw training data.",
    "Fewer iterations can limit overtraining, but do not specifically encourage sparse coefficients or remove irrelevant feature effects.",
    "Debugger diagnoses training behavior. It does not retrofit L1 regularization into a running model's optimization objective."
  ],
  "510": [
    "Similarity encoding maps similar category strings to similar low-dimensional representations, helping with high-cardinality categories that contain spelling noise.",
    "Ordinal encoding assigns arbitrary ordered numbers to these names and does not make spelling variants semantically close.",
    "Data Wrangler similarity encoding uses string similarity to keep variants such as a correctly spelled name and a typo close in the encoded space.",
    "One-hot encoding creates separate independent columns for spelling variants, increasing dimensionality and failing to express their similarity.",
    "Target encoding summarizes labels by category. It does not inherently merge spelling variants and requires careful leakage control."
  ],
  "511": [
    "An excessive learning rate and noisy gradient estimates can cause unstable optimization. Smaller updates and a larger mini-batch can stabilize the training trajectory.",
    "Increasing the learning rate enlarges updates, while smaller batches usually increase gradient noise. Both can worsen oscillation.",
    "Raising the learning rate over time can make an already unstable optimizer overshoot further.",
    "Lower the learning rate and increase the mini-batch size within memory limits to reduce update magnitude and gradient noise. Validate generalization separately; stable convergence alone does not guarantee it.",
    "Lowering the learning rate may help, but reducing the batch size adds gradient noise and is less targeted to the observed oscillation than C."
  ],
  "512": [
    "EFS provides a shared NFS file system for Linux clients, with network access from AWS and connected on-premises environments and NFS locking semantics.",
    "Mountpoint exposes S3 objects through a file interface but does not provide the full shared NFS locking behavior required by the producers.",
    "Mount EFS over supported NFS connections from the notebooks and on-premises servers, using VPN or Direct Connect and appropriate network rules. This preserves a shared NFS-oriented workflow.",
    "FSx for Lustre is a high-performance Lustre file system requiring compatible clients. It is not a drop-in NFS shared file system for the stated environment.",
    "EBS is block storage attached to supported EC2 instances, not a shared NFS service that on-premises servers and notebooks can all mount directly."
  ],
  "513": [
    "DataBrew recipes apply visual data transformations through managed jobs, including categorical encoding and S3 output.",
    "Create the one-hot encoding recipe visually and run a recipe job over the Parquet dataset. Write to a separate output prefix in the same bucket to avoid mixing source and transformed records.",
    "Athena can express transformations in SQL, but authoring SQL does not meet the no-code requirement.",
    "An interactive Glue notebook requires code to define and execute the transformation.",
    "Redshift adds a database setup and SQL implementation when a visual managed transformation is sufficient."
  ],
  "514": [
    "SageMaker script mode runs user-provided training scripts inside supported framework containers, preserving much of an existing framework workflow.",
    "Built-in algorithms replace the training implementation with predefined algorithms rather than running the company's custom PyTorch script.",
    "Canvas offers a visual model-building workflow but is not the direct path for reusing arbitrary existing PyTorch training scripts.",
    "JumpStart accelerates deployment and customization of supported models and solutions. It does not primarily provide execution of the company's existing custom training code.",
    "Use the PyTorch framework container with script mode and adapt data paths, arguments, and model output locations as needed. This minimizes changes to the existing code."
  ],
  "515": [
    "QuickSight anomaly detection identifies unexpected deviations in time-series measures. A display transformation can emphasize large positive values but is not the detector itself.",
    "A square-root transform compresses large positive differences, and a custom Lambda detector adds unnecessary implementation effort.",
    "Removing outliers from the data does not display them in the existing dashboard. Writing removed rows to DynamoDB creates a separate workflow.",
    "Use QuickSight anomaly insights to identify unexpectedly high prices and display the flagged observations. Squaring positive prices can exaggerate visual differences, but is optional and changes the displayed units; label the chart accordingly.",
    "Selecting the ten lowest prices addresses the opposite tail and does not identify unexpectedly high values."
  ],
  "516": [
    "Changing anomaly direction filters which tail is reported. Restricting alerts to low temperatures removes high-temperature anomalies from the results.",
    "The narrower direction cannot increase the set of reported anomalies under the same severity setting, and it omits true high-temperature events.",
    "Fewer anomalies are reported, and recall across both high and low abnormal temperatures decreases when true high-temperature anomalies are excluded. If no high anomalies occur, the measured counts may remain unchanged.",
    "Recall may decrease, but the reporting frequency does not increase merely by filtering out the upper tail.",
    "Frequency can decrease, but recall over the original two-sided target cannot improve by discarding detected high-temperature cases."
  ],
  "517": [
    "Container images and Kubernetes workload definitions can be moved to EKS without redesigning the underlying ML services.",
    "Rewriting services for Kubeflow is a platform redesign rather than a minimal lift and shift.",
    "Push the existing images to ECR and adapt the Kubernetes deployment pipeline for EKS, including IAM, storage, networking, and secrets. The existing service packaging can remain largely intact.",
    "Redshift ML changes the training platform and requires data migration and retraining that the requested cluster migration does not need.",
    "Retraining through notebooks is unnecessary for moving already packaged training and inference services to EKS."
  ],
  "518": [
    "Document metadata filters constrain retrieval before answer generation, allowing freshness rules for a selected category of questions.",
    "Index the relevant document date and apply a last-month attribute filter when handling price questions. Keep broader retrieval for general questions and ensure the filter is enforced by the application.",
    "Source citations identify supporting documents after retrieval; they do not by themselves exclude outdated documents.",
    "Q Developer is oriented toward software development, and folder organization alone does not implement the required customer-document retrieval rule.",
    "Manual folder permissions are a coarse access control and Q Developer is not the intended business knowledge assistant. This is less direct than a query-specific date filter."
  ],
  "519": [
    "Sparse recommendation features suit Factorization Machines when the target is a binary interaction or a numeric preference score. A direct airport identifier target is a different multiclass problem.",
    "CatBoost is a supported tabular algorithm and can handle multiclass labels. It cannot be dismissed merely because the input has categorical features; the appropriate choice depends on the target formulation.",
    "DeepAR forecasts numerical time series. It does not directly predict one airport among thousands of categorical destinations.",
    "This is appropriate after constructing user-airport pairs and learning interaction scores, then ranking airports. SageMaker Factorization Machines does not directly support a 2,000-class airport target, so the source answer needs this missing reformulation.",
    "Clustering can group users but does not learn the supervised next-airport target directly. Mapping clusters to destinations requires additional logic and loses individualized ranking information."
  ],
  "520": [
    "Scaling from zero is reactive unless capacity is scheduled in advance. Provisioning an instance after a failed invocation still causes startup delay.",
    "A shorter cooldown does not create capacity before a request arrives. EC2 Auto Scaling lifecycle hooks are not the control mechanism for SageMaker inference component copies.",
    "With zero instances, there is no running instance CPU utilization to trigger the desired proactive capacity change.",
    "Lowering the per-copy request target does not provide an advance signal while no copies are running.",
    "A no-capacity alarm and step policy can recover from zero, but cannot guarantee a ready endpoint at the start of the day. Use scheduled scaling before business hours or a nonzero minimum; an alarm does not itself schedule a minimum-capacity change."
  ],
  "521": [
    "Detailed profiling correlates CPU/GPU work with labeled regions of training code, helping identify compute, communication, and input bottlenecks.",
    "Aggregate GPU metrics show underutilization over time but do not precisely attribute it to a section of the training script.",
    "Add Profiler annotations around relevant training regions and inspect the resulting timeline/report to locate GPU idle time or communication bottlenecks. Use a supported Profiler environment and check its service lifecycle.",
    "CloudTrail records API calls. It does not capture a GPU execution timeline or GPU memory utilization profile.",
    "Model Monitor compares production data or predictions with baselines. It does not profile DDP training code."
  ],
  "522": [
    "Data quality drift includes changes in production input distributions relative to a training reference. Predictive quality and attribution drift are separate monitoring targets.",
    "Compare current feature statistics and distributions with the training-data baseline to detect the customer-behavior distribution changes described.",
    "Model quality drift measures prediction performance against actual outcomes. It requires ground truth and answers a different question from input distribution change.",
    "Bias drift compares disparities across configured groups. It does not cover all input distribution changes.",
    "Attribution drift concerns changes in feature importance for predictions, which can differ from changes in the raw feature distributions."
  ],
  "523": [
    "Asynchronous inference accepts an S3 input location, processes the request in the background, stores the result in S3, and supports SNS completion notifications.",
    "Batch transform is an offline batch workflow, and SQS is a queue for consumers rather than a direct user notification channel.",
    "Store each image in S3, submit its location to the asynchronous endpoint, and configure an SNS success or failure topic. Route the completion event to the appropriate user notification channel.",
    "EFS adds file-system management and is not the standard batch transform input location. SQS also requires a separate notification consumer.",
    "SNS is appropriate, but an asynchronous inference request references input in S3 rather than an EFS file path."
  ],
  "524": [
    "When missing a positive case is more costly than a false alert, recall receives greater emphasis than precision, subject to agreed validation and operational limits.",
    "The higher recall aligns with the stated tolerance for false positives. Promote the candidate after confirming its absolute acceptance thresholds, subgroup results, and operational readiness, then continue monitoring.",
    "Collecting more data can improve confidence, but waiting alone does not cause a fixed model's precision to improve and does not address the stated preference for recall.",
    "A gradual rollout can limit exposure during deployment, but traffic allocation does not intrinsically reduce the model's prediction errors or resolve the metric tradeoff.",
    "Rejecting the candidate solely for lower precision ignores the explicit priority of detecting more readmissions while accepting false positives."
  ],
  "525": [
    "SageMaker's predefined high-resolution scaling metrics are ConcurrentRequestsPerModel and ConcurrentRequestsPerCopy. InvocationsPerInstance is a standard one-minute metric.",
    "Configuring a ten-second evaluation does not make the standard InvocationsPerInstance metric publish ten-second data.",
    "This incorrectly calls InvocationsPerInstance a high-resolution metric. A longer scale-in cooldown also does not accelerate scale-out for a traffic spike.",
    "The standard metric still has one-minute publication, and the scale-in cooldown governs capacity removal rather than reaction speed to increasing traffic.",
    "This is the source answer, but it names the wrong metric. Use the supported high-resolution concurrent-request metric for ten-second publication; none of the options states that configuration correctly."
  ],
  "526": [
    "Oversampling can balance training classes without deleting original records. A signed imbalance measure depends on which category is assigned to each comparison group.",
    "A zero imbalance value indicates equal group counts, so rebalancing is unnecessary. Undersampling also discards existing records, violating the requirement.",
    "When the configured positive imbalance indicates a majority group, SMOTE can add minority-class training examples without dropping originals. Inspect both signs of imbalance, and apply resampling only to the training partition with an appropriate feature representation.",
    "JumpStart is not the bias-reporting tool described, and undersampling removes original data.",
    "JumpStart is not the specified imbalance analysis tool, and a zero imbalance value does not justify oversampling."
  ],
  "527": [
    "Nominal categories need an encoding without artificial order. An ordinal category can use an explicit rank when that order has meaning.",
    "Tokenizing a three-valued location field treats a structured category as free text and adds unnecessary complexity.",
    "Use one-hot indicators for location and an explicit seniority rank for job level. In logistic regression, validate the implied relationship between rank increments and log odds; use a richer encoding if it is not appropriate.",
    "Binning is mainly for continuous values, and standard scaling cannot directly process the string job-level categories.",
    "One-hot encoding is suitable for location, but job seniority strings must be encoded before numerical standard scaling can be applied."
  ],
  "528": [
    "A managed real-time endpoint with auto scaling supports synchronous low-latency inference while adapting provisioned capacity to demand.",
    "Batch transform processes offline batches rather than responding to individual near-real-time requests.",
    "S3 stores model artifacts but does not execute model code to serve predictions.",
    "Deploy the model to a real-time SageMaker endpoint and configure capacity limits, target tracking, and suitable warm capacity for the latency requirement.",
    "Two fixed EC2 instances behind a load balancer do not automatically scale for varying request volumes and require more serving infrastructure management."
  ],
  "529": [
    "Distributed data parallelism divides training batches across workers and synchronizes gradients. Additional instances can reduce elapsed time when computation scales efficiently.",
    "More epochs perform more passes over the dataset and generally increase training time.",
    "More hidden neurons increase computation and memory use rather than reducing the work of the existing model.",
    "Additional layers increase model computation and can make each training step slower.",
    "Increase the number of compatible instances so more training examples are processed in parallel. Benchmark communication overhead, global batch size, convergence, and cost because speedup is not guaranteed to be linear."
  ],
  "530": [
    "Bayesian optimization builds a surrogate model of objective performance and uses previous trials to choose promising next hyperparameters.",
    "Grid search follows a predefined combination grid instead of adapting to previous results through a surrogate model.",
    "Random search samples independently and does not use previous objective values to guide the next choice.",
    "Bayesian optimization uses a probabilistic regression model and acquisition strategy to choose subsequent trials, matching the sequential, informed search described.",
    "Hyperband allocates budgets to configurations and stops poor trials early. Its defining mechanism is successive resource allocation rather than regression-guided sequential selection."
  ],
  "531": [
    "Random Cut Forest assigns anomaly scores to unlabeled numerical feature vectors. Ensemble size controls a tradeoff between score stability and computation.",
    "Supervised XGBoost needs a target for this classification framing. Setting tree depth above 100 would not solve missing labels and can greatly increase complexity.",
    "K-means can support distance-based anomaly heuristics, but it is not the built-in anomaly scoring algorithm most directly suited to the stated task.",
    "DeepAR is a time-series forecasting algorithm. The question does not define the forecasting target and time-series structure needed for it.",
    "RCF is designed for unsupervised anomaly detection. More than 100 trees can form a larger ensemble, but that threshold is not a universal accuracy guarantee; validate tree count and preprocessing on the application."
  ],
  "532": [
    "Early stopping halts training when held-out performance stops improving and can restore the best checkpoint before overfitting grows.",
    "Additional layers increase capacity and can worsen the later-stage overfitting behavior.",
    "Monitor an appropriate validation metric, stop after a defined patience interval without improvement, and retain the best model checkpoint.",
    "Adding neurons increases capacity rather than preventing training from continuing past the best generalization point.",
    "Bias/variance analysis can diagnose the cause, but analysis alone does not stop the degrading training process."
  ],
  "533": [
    "Imbalanced image classification benefits from minority-class-aware evaluation and realistic, label-preserving image augmentation.",
    "Minority augmentation is appropriate, but overall accuracy can remain high even when the minority class is largely missed.",
    "Use a minority-class or macro F1 objective and augment minority images with valid transformations. Evaluate on untouched held-out data with the natural class distribution.",
    "Accuracy can hide minority failure, and ordinary SMOTE on raw image pixels can create unrealistic images that do not preserve meaningful visual structure.",
    "F1 is suitable, but direct pixel-space SMOTE is usually less appropriate than label-preserving image transformations. SMOTE in a validated embedding space would be a different, explicitly designed method."
  ],
  "534": [
    "A vector knowledge base requires an embedding model to map extracted document chunks into vectors. A text-generation model is not interchangeable with an embedding model.",
    "Titan Text Premier is a text-generation model. The corresponding embedding task requires a supported embedding model, not this generator.",
    "Instruction tuning changes a model's behavior through training. It does not perform document embedding for the vector index.",
    "Claude instruction tuning is not the document-to-vector step required to ingest the PDFs.",
    "Cohere Embed Multilingual is an embedding model that can encode extracted PDF text chunks for a compatible Bedrock knowledge base vector index. Verify current model and Region availability."
  ],
  "535": [
    "Distributed Glue Spark processing can scan text fields for sensitive entities and apply managed redaction. Unstructured files must first be parsed into supported text records.",
    "Use Glue's Spark engine and Detect PII redaction actions to process partitioned text data at scale. Develop interactively, then operationalize suitable jobs; extract text from non-text documents before detection.",
    "The option conflates Glue and SageMaker Data Wrangler. A visual preparation session is not the specified distributed petabyte-scale PII processing pipeline.",
    "Clarify analyzes bias and feature attribution. It is not a PII detection and masking API.",
    "DetectEntities identifies general entities rather than providing the requested complete PII redaction. Detection APIs return metadata; explicit masking and large-scale orchestration would still be necessary."
  ],
  "536": [
    "Production constraints and operational errors can signal problems, but validating recommendation quality requires defined predictive metrics and appropriate outcomes or labels.",
    "Clarify focuses on bias and attribution, not a general raw feature distribution check. Scheduled Model Monitor jobs also are not per-request inline validation.",
    "Endpoint dashboards and attribution drift can inform diagnosis but do not by themselves measure the quality of recommendations after a campaign changes user behavior.",
    "Bias and latency monitoring address narrower risks and do not establish whether recommendations remain accurate or useful.",
    "This is the intended monitoring and diagnostics combination, but constraint checks and error logs alone do not validate predictive performance before a campaign. Add model-quality or business-outcome evaluation and representative pre-campaign testing; the key remains conditional."
  ],
  "537": [
    "Feature scaling, seasonal patterns, regional population size, and validation design are distinct issues. The described recurring peaks do not identify which one causes low accuracy.",
    "Recurring high sales can be genuine seasonal behavior. Removing every outlier can erase useful signal and is not justified by the facts provided.",
    "Stratifying by month and region can improve representation, but time-series evaluation may instead require a chronological split. The existing split is not described.",
    "Global scaling can help scale-sensitive models, but does not remove seasonal patterns or regional size effects. It has little effect on ordinary tree split ordering.",
    "Per-region normalization can reduce regional scale differences, but cannot be assumed to fix seasonal peaks or the unspecified model. The source's definitive claim needs more information about the target, algorithm, and split."
  ],
  "538": [
    "The defect class is the minority: 200 defect images versus 1,800 non-defect images. Rebalancing should increase minority representation or reduce the majority, not shrink the minority.",
    "Changing to a managed service and an arbitrary smaller dataset does not specifically address the identified imbalance.",
    "Removing some of the 200 defect images makes the minority representation even smaller and can discard useful defect diversity.",
    "Oversample the defect training images, preferably with valid augmentation where appropriate. Keep evaluation images separate to avoid leakage from duplicated examples.",
    "Using the same imbalanced dataset with a different service does not by itself rebalance the classes."
  ],
  "539": [
    "Data Wrangler provides integrated connectors for importing and preparing data from S3, Athena, Snowflake, and other supported sources.",
    "DataBrew is a separate preparation service. It is not the direct SageMaker import interface spanning the listed source connections.",
    "Pipelines orchestrates jobs and DataSync transfers supported storage data. This adds a custom workflow and does not replace the database query connectors.",
    "The Feature Store Spark connector ingests already prepared DataFrames into feature groups. It is not a general interactive query/import connector for these three sources.",
    "Use the native Data Wrangler connections to query or select each source and import the results into a preparation flow. Configure the required credentials and network access."
  ],
  "540": [
    "Least-privilege permissions should match each application action: receive work, invoke the image model, and delete completed work.",
    "bedrock:InvokeModel authorizes generating an image with the specified model resource through the runtime API.",
    "Get operations read metadata or configuration. They do not authorize a model inference invocation.",
    "sqs:ReceiveMessage reads a queued task, and sqs:DeleteMessage removes it after successful processing. Scope both permissions to the intended queue.",
    "GetQueueAttributes reads queue configuration, not message contents. ReceiveMessage is still required to obtain the task.",
    "SageMaker PutRecord actions concern Feature Store ingestion, not Bedrock image generation."
  ],
  "541": [
    "Git pull fetches remote changes and integrates them into the current local branch, updating the notebook files in the working tree.",
    "Pull from the repository's actual shared branch to obtain the latest merged notebook. The command shown assumes that branch is named master; use main or another configured name when appropriate.",
    "Commit records local changes. It does not retrieve other contributors' remote commits.",
    "Push uploads local commits to the remote repository, which is the opposite direction from obtaining collaborators' updates.",
    "Branch lists or manages branch references. It does not fetch and integrate remote changes by itself."
  ],
  "542": [
    "DeepAR learns shared temporal patterns across related numerical time series and can condition forecasts on categorical features.",
    "scale_pos_weight addresses binary class imbalance in XGBoost, not the time-series context and forecast horizon described.",
    "K-means clusters observations. It does not model the ordered history needed to forecast future numerical values.",
    "DeepAR is the appropriate forecasting algorithm. Starting with context_length similar to prediction_length is a tuning heuristic, not a mandatory equality; choose both based on the horizon and validation results.",
    "RCF detects anomalies rather than forecasting the future target sequence. contamination is not the stated SageMaker built-in RCF hyperparameter."
  ],
  "543": [
    "Use synchronous managed inference for immediate predictions and a separate long-running processing job for historical evaluation. Scaling must reference a supported metric.",
    "Real-time endpoints with target tracking fit the online workload, but ConcurrentInvocationsPerinstance is not the documented metric name. Use a supported metric such as InvocationsPerInstance or the high-resolution ConcurrentRequestsPerModel.",
    "Reserved concurrency limits Lambda execution capacity, and SnapStart reduces supported Lambda startup time. It is not a connector to SageMaker or the endpoint's scaling policy.",
    "A scheduled SageMaker Processing job can run the multi-hour historical analysis and compare predictions with actual outcomes without tying up online serving capacity.",
    "An EC2 Auto Scaling group can run custom analysis workers, but needs explicit batch orchestration and host management. It is not necessary when Processing provides the managed job runtime.",
    "A single Lambda invocation is limited to 15 minutes, which is shorter than the several-hour analysis. The intended A/C pair requires correcting A's metric name."
  ],
  "544": [
    "Strong training performance with weak validation performance suggests overfitting or a distribution mismatch. XGBoost's lambda parameter adds L2 regularization to make fitting more conservative.",
    "More representative data can help if the training population is inadequate, but collecting and preparing it generally takes more effort than tuning an existing regularization parameter.",
    "Increasing boosting rounds can fit the training set more closely and worsen overfitting unless controlled by validation and early stopping.",
    "Error is a classification evaluation metric and is inappropriate for this continuous house-price regression task. Changing a metric alone does not improve predictions.",
    "Increase and validate lambda to penalize excessive weight magnitudes and reduce overfitting. This is the least-effort targeted change among the choices; confirmed data distribution mismatch would additionally require representative data."
  ],
  "545": [
    "Online recommendation experiments should measure the user action the product aims to cause, such as a purchase or another defined conversion.",
    "Compare conversion rates between randomly assigned groups, using a consistent attribution window and statistical uncertainty. This directly measures whether users act on recommendations.",
    "Recommendation count measures exposure, not whether the user followed or acted on the recommendation.",
    "Offline test accuracy can be useful, but does not directly measure user behavior in the live randomized experiment.",
    "Inference latency affects experience but is an operational metric rather than the requested action outcome."
  ],
  "546": [
    "A demographic bias report compares outcomes across specified sensitive groups and documents the selected fairness metrics and evaluation data.",
    "Configure Clarify with the demographic facets and relevant labels or predictions, generate the bias report, and provide its assumptions and interpretation to the auditor.",
    "Data-quality drift concerns changes in inputs. It is not a demographic fairness assessment of the model.",
    "A dashboard tool can display computed metrics but does not automatically generate the required bias analysis merely through a SageMaker integration.",
    "Default endpoint CloudWatch metrics measure serving behavior. They do not establish demographic bias without a configured bias monitoring or analysis job."
  ],
  "547": [
    "Managed image moderation can analyze sampled frames from a live video pipeline. The application must define frame extraction, latency, and coverage requirements.",
    "Extract suitable frames and use Rekognition moderation APIs through a Lambda-driven workflow. This avoids training and operating a custom moderation model, while sampling and throughput must be sized for the stream.",
    "Adding a Bedrock LLM introduces extra inference and integration when Rekognition's moderation labels meet the stated need.",
    "A custom SageMaker vision model requires training data, model evaluation, hosting, and maintenance beyond the managed moderation workflow.",
    "Transcribe analyzes audio and Comprehend analyzes text. They do not inspect the visual content of video frames."
  ],
  "548": [
    "CloudWatch exposes SageMaker endpoint invocation, latency, and error metrics. Logs can supply additional application details when needed.",
    "X-Ray is useful for tracing instrumented application paths, but it is not the simplest source for the endpoint's existing system metrics.",
    "Use CloudWatch endpoint metrics and dashboards for latency, volume, and error classes. Add a Lambda log processor only if exact status-code or custom fields are absent from native metrics; it is not required for every dashboard.",
    "IAM Access Analyzer evaluates resource access and policies. It does not collect endpoint performance time series.",
    "Trusted Advisor offers account and resource recommendations. It is not a per-invocation endpoint metric dashboard."
  ],
  "549": [
    "Bedrock Custom Model Import accepts supported model architectures and required Hugging Face artifacts, then exposes managed on-demand inference.",
    "Use the imported model's supported on-demand runtime invocation path. The application can incorporate those calls into its agent logic; verify the model's supported APIs and capabilities.",
    "Quantization is not a universal prerequisite and an arbitrary quantized format may not meet the import requirements. Supply the documented supported weights and configuration.",
    "The application must create a Bedrock import job through an appropriate Bedrock API or console workflow. Merely using the SageMaker SDK does not import a Hugging Face model into Bedrock.",
    "A customer-configured SageMaker-style auto scaling policy is not required during Bedrock import. Bedrock manages the imported model's serving capacity.",
    "Provide the S3 location containing all required supported model files and grant the import role access. A same-account bucket works, although current Bedrock documentation also permits properly authorized cross-account S3 sources."
  ],
  "550": [
    "In the SageMaker Python SDK Estimator interface, fit submits a training job with the supplied data channels.",
    "Call the configured estimator's fit method with the training data location or channel mapping to initiate training. This refers to the Estimator-style SDK interface used in the question.",
    "create_model constructs a deployable model definition from artifacts and serving configuration. It does not train the model.",
    "deploy provisions a serving endpoint for a model. It is a hosting operation after training.",
    "predict sends inference input to a predictor. It does not initiate parameter learning on a training dataset."
  ]
}
