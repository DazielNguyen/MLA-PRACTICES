import { ArrowRight, BookOpen, Clock3, Download, HardDrive, Upload } from 'lucide-react';
import type { Question, State } from './domain';
import { formatTime, score } from './domain';
import { Empty } from './components';
export default function ProgressPage({bank,state,go,exportProgress,importProgress}:{bank:Question[];state:State;go:(p:string)=>void;exportProgress:()=>void;importProgress:()=>void}) {
  const wrong=Object.values(state.progress).filter(p=>!p.latest).length;
  return <div className="page progress-page"><div className="page-heading"><div className="eyebrow">SMALL STEPS ADD UP</div><h1>Nhìn lại để tiến xa hơn.</h1><p>Mỗi phiên học đều có ích. Theo dõi kết quả và dành thời gian cho câu còn khó.</p></div><div className="progress-overview"><div className="panel"><span>Phiên đã hoàn thành</span><strong>{state.history.length}</strong><small>Lưu tối đa 100 phiên gần nhất</small></div><div className="panel"><span>Câu cần luyện lại</span><strong>{wrong}</strong><button className="text-button" onClick={()=>go('/practice')}>Luyện với bộ lọc câu sai <ArrowRight size={14}/></button></div><div className="panel"><span>Flashcard đã thuộc</span><strong>{state.known.length}<small> / 332</small></strong><button className="text-button" onClick={()=>go('/flashcards')}>Tiếp tục ôn thẻ <ArrowRight size={14}/></button></div></div><section className="panel backup-panel"><span className="mode-icon green"><HardDrive size={24}/></span><div><h2>Tiến trình của bạn, trên thiết bị của bạn.</h2><p>Tự động lưu trong trình duyệt, không cần đăng nhập. Xuất bản sao để chuyển thiết bị hoặc giữ lại trước khi xóa dữ liệu trình duyệt.</p></div><div className="backup-actions"><button className="button primary" onClick={exportProgress}><Download size={16}/>Xuất bản sao JSON</button><button className="button secondary" onClick={importProgress}><Upload size={16}/>Nhập bản sao</button></div></section><div className="section-heading"><h2>Lịch sử học tập</h2><span>{state.history.length} phiên</span></div>{state.history.length?<div className="panel history-table"><div className="history-table-heading"><span>PHIÊN HỌC</span><span>THỜI GIAN</span><span>KẾT QUẢ</span><span/></div>{state.history.map(s=>{const result=score(s,bank);return <button className="history-row" key={s.id} onClick={()=>go(`/results/${s.id}`)}><span className="history-title"><span className="recent-icon">{s.mode==='exam'?<Clock3 size={19}/>:<BookOpen size={19}/>}</span><span><strong>{s.mode==='exam'?'Thi thử':'Luyện câu hỏi'} · {s.questionIds.length} câu</strong><small>{new Date(s.finishedAt!).toLocaleString('vi-VN')}{s.finishReason==='timeout'?' · Tự nộp khi hết giờ':''}</small></span></span><span>{formatTime(s.finishedAt!-s.startedAt)}</span><span className="history-score"><strong>{result.total?`${result.percent}%`:'—'}</strong><small>{result.correct}/{result.total} câu đúng</small></span><ArrowRight size={17}/></button>;})}</div>:<div className="panel"><Empty title="Chưa có phiên đã hoàn thành">Hoàn thành một phiên luyện hoặc thi thử để xem kết quả tại đây.</Empty></div>}</div>;
}
